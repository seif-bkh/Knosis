/// How a stretch of reading was done (PRODUCT_REPORT.md section 8.4).
///
/// Stored as the enum name, so adding a mode later does not renumber
/// existing rows.
enum ReadingMode { flow, study, review }
